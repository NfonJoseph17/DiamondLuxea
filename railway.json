{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "corepack enable && corepack prepare pnpm@9.14.2 --activate && pnpm install --frozen-lockfile && pnpm --filter api prisma:generate && pnpm --filter api build"
  },
  "deploy": {
    "startCommand": "pnpm --filter api start:prod",
    "preDeployCommand": "pnpm --filter api prisma:migrate:deploy"
  }
}
